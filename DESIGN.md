# Дизайн-система мини-аппа

Единый свод правил для всего интерфейса. Источник — макет из трёх экранов
(Главная / Тренировка / Прогресс), утверждённый 2026-08-29.

**Как этим пользоваться.** Перед любой правкой интерфейса читаем этот файл и
берём значения отсюда, а не «на глаз». Если нужного значения тут нет — сначала
дописываем правило сюда, потом делаем. Новых цветов, размеров и радиусов вне
этих шкал в коде быть не должно.

**Статус:** спецификация. В коде на 2026-08-29 ещё НЕ реализована.
Заменяет собой бежевый макет от 25.08 (там были засечные заголовки и фон
`#f6f1ea` — от них отказались в пользу светло-серого фона).

**Одно отличие от макета.** Макет нарисован с золотым акцентом; акцент
заменён на приглушённый петроль (тёмную бирюзу) — золото решили не брать.
Всё остальное — компоновка, отступы, карточки, шрифт, иконки, графики —
берётся из макета один в один.

---

## 1. Общий характер

Светлое, спокойное, «дорогое» приложение. Четыре опоры:

1. **Светлый нейтральный фон, белые карточки.** Интерфейс — это стопка белых
   карточек на светло-сером листе. Никаких цветных заливок во всю ширину.
2. **Много воздуха.** Отступы крупные и одинаковые. Если экран выглядит
   «дёшево» — почти всегда не хватает воздуха, а не цвета.
3. **Цвет — специя, а не краска.** Акцентом красится только то, что требует
   действия или отмечает достижение. На любом экране акцента должно быть
   заметно меньше, чем белого и серого. Ориентир — не больше 3 акцентных
   элементов на экран.
4. **Мягкость.** Крупные скругления, размытые полупрозрачные тени, тонкие
   светлые границы. Ни одной жёсткой чёрной линии.

---

## 2. Цвета

### 2.1 Фон и поверхности

| Токен | Значение | Где |
|---|---|---|
| `--page-bg` | `#F4F4F6` | фон всего экрана под карточками |
| `--surface` | `#FFFFFF` | карточки, панели, нижняя навигация, шапка |
| `--surface-2` | `#F1F1F4` | «утопленные» области: дорожка прогресс-бара, фон сегмент-контрола, серые чипы |
| `--surface-3` | `#FAFAFA` | вложенная карточка внутри белой карточки |
| `--surface-4` | `#E9E9ED` | разделители-плашки, скелетоны загрузки |

### 2.2 Границы

| Токен | Значение | Где |
|---|---|---|
| `--border` | `#EAEAEE` | обводка карточек, полей ввода, чипов |
| `--border-soft` | `#F2F2F5` | разделители строк внутри карточки |

Граница **всегда** 1px и всегда светлее фона карточки не более чем на пару
тонов. Толстых и тёмных рамок нет нигде.

### 2.3 Текст

| Токен | Значение | Где |
|---|---|---|
| `--text` | `#15171B` | заголовки, цифры, основной текст |
| `--text-muted` | `#6C7078` | подписи под цифрами, вторичные строки, неактивные вкладки |
| `--text-faint` | `#A0A4AB` | подписи осей графика, самые мелкие пояснения |
| `--text-on-accent` | `#FFFFFF` | текст на акцентной заливке |

### 2.4 Акцент — шкала

Акцент — приглушённый петроль (тёмная бирюза). Одного оттенка мало: у цвета
шесть ролей, и ступени подобраны так, чтобы контраст держался в каждой.

| Токен | Значение | Роль |
|---|---|---|
| `--accent-50` | `#EDF5F4` | фон выбранного/акцентного блока (цель, достижение, выбранный день) |
| `--accent-100` | `#DCEBE9` | вторая точка мягкого градиента на таких блоках |
| `--accent-200` | `#B9D9D5` | граница выбранного блока |
| `--accent-300` | `#2A8B84` | осветлённый: старт градиента кнопки |
| `--accent-500` | `#16706B` | **база**: иконки, линия графика, активная вкладка, заливка прогресс-бара, кнопка поверх фото |
| `--accent-600` | `#0E5A56` | финиш градиента кнопки, состояние нажатия |
| `--accent-700` | `#0B4A47` | акцентный **текст** на светлом фоне (`accent-50`) — только он читается |

**Правило контраста.** Текст на акценте:
- на любой заливке от `accent-300` до `accent-600` — белый;
- на `accent-50` (тинт-плашка) — тёмный `--text`, а если нужен именно
  акцентный, то только `accent-700`. `accent-500` текстом на светлом фоне не
  используем — не читается.

Светлой кнопки с тёмным текстом в системе нет: акцент тёмный, и текст на нём
всегда белый. Это упрощает жизнь и при смене цвета тренером.

### 2.5 Статусные цвета

| Токен | Значение | Роль |
|---|---|---|
| `--fg-success` | `#2E9E4F` | рост показателя, «+20%», выполнено, снижение веса |
| `--fg-danger` | `#D24B3C` | падение показателя, ошибки, удаление |
| `--fg-warning` | `#B0791F` | предупреждения |
| `--fg-info` | `#3D6C97` | нейтральные подсказки |
| `--tint-success` | `#EAF4EA` | фон под success |
| `--tint-danger` | `#FBEBE9` | фон под danger |
| `--tint-warning` | `#FBF2E0` | фон под warning |
| `--tint-info` | `#EAF0F6` | фон под info |

Динамика всегда идёт с треугольником-стрелкой и знаком: `▲ +20%`, `▼ −0,2 кг`.
Цвет определяется **смыслом, а не знаком**: снижение веса — зелёное.

Зелёный `--fg-success` намеренно травянистый, а не бирюзовый: он обязан
отличаться от акцента, иначе «выполнено» и «нажми сюда» сливаются.

### 2.6 Тёмная плашка

| Токен | Значение | Роль |
|---|---|---|
| `--ink` | `#1E2126` | тултип графика, всплывающие подсказки |

### 2.7 Мульти-тенантность (важно)

Акцент подставляется под каждого тренера из админки (`applyTenantTheme`
ставит `--color-primary`). Поэтому:

- ни один хекс акцента **не пишется в CSS напрямую** — только через токены
  `--accent-*`;
- петроль — значение по умолчанию, а не константа;
- когда тренер ставит свой цвет, из него должны выводиться все шесть ступеней
  шкалы (осветление/затемнение), а не подменяться одна `accent-500`;
- если тренер поставит светлый цвет, `accent-500..700` затемняются
  принудительно: текст на акценте у нас всегда белый, и он обязан читаться.

### 2.8 Тёмная тема

Тема `theme-dark` переопределяет **только фон, текст и тени**:

| Токен | Тёмная тема |
|---|---|
| `--page-bg` | `#0E0F11` |
| `--surface` | `#17191D` |
| `--surface-2` | `#1F2228` |
| `--border` | `#26292F` |
| `--text` | `#F2F3F5` |
| `--text-muted` | `#9BA0A8` |

**Акцент в этом блоке не переопределяется — никогда.** Раньше здесь стояли
свои значения `--accent-*`, и они ломали фирменный цвет тренера: JS ставит
шкалу на `html`, а объявление внутри `body.theme-dark` живёт на `body` и
перебивает унаследованное значение для всего содержимого страницы. В итоге
клиенты любого тренера на тёмной теме видели чужой цвет.

Яркость под тёмный фон подбирает `_applyAccentScale()` из того же
`theme.primary`: база подсвечивается до читаемой, мягкие ступени становятся
полупрозрачными, акцентный текст — светлым. Подробности — раздел 2.11.

Тени в тёмной теме почти не видны — вместо них поверхности разделяются
границами. Значения теней уменьшаем по прозрачности вдвое.

### 2.9 Запрет золотого и жёлтого

**Петроль — единственный основной акцент приложения.** Золотой, жёлтый и
янтарный в интерфейсе не используются ни для чего: ни для рекордов, ни для
важных пометок, ни для выделения выбранного элемента. Всё это красится
акцентом.

Единственное исключение — **статус «предупреждение»** (`--fg-warning`,
`--tint-warning`). Это не акцент, а сигнал состояния, и он обязан оставаться
отдельным цветом.

Статусные цвета (успех, ошибка, предупреждение, информация) не сливаются с
акцентом и друг с другом. Зелёный успеха специально травянистый `#2E9E4F`,
а не бирюзовый: иначе «выполнено» и «нажми сюда» читаются одинаково.

**Что ещё осталось жёлтым в коде** (на 2026-08-29 не тронуто; заменяется на
том шаге, когда доходим до соответствующего экрана):

| Место в `style.css` | Что это | Чем заменить |
|---|---|---|
| `.food-log-btn` — градиент `#FF9800 → #F57C00` | кнопка записи еды в карточке клиента | градиент акцента `accent-300 → accent-600` |
| `.cc-readonly-banner` — `#FBC02D` | плашка «режим только для чтения» | статус: `--fg-warning` |
| `.finance-client-card.fc-expiring_week` — `#ffd54f` | абонемент истекает на неделе | статус: `--fg-warning` |
| `.ex-stat-pr` — `#FFC107` | личный рекорд в статистике упражнения | акцент `--accent-500` |
| `.stats-pr-row` — `#FFC107` | строка рекорда в статистике | акцент `--accent-500` |
| `.notes-item-important` — `#FFC107` | важная заметка | акцент `--accent-500` |
| `.prof-chk:has(input:checked)` — `#FFC107` | выбранный пункт в профиле | акцент `--accent-500` + фон `--accent-50` |
| ещё 3 места с `#FBC02D` / `#FFC107` (границы слева) | пометки в тренерской | по смыслу: статус или акцент |

Правило на будущее: акцентный хекс в селекторе не пишется никогда — только
через токены `--accent-*`, иначе тенантная тема перестанет работать.

### 2.10 Общие классы и границы клиент / тренер

В приложении два интерфейса: клиентский (Главная, Тренировка, Прогресс,
История, Замеры) и тренерский (Админ, Тренеры, карточка клиента, мастер
нового клиента и модалки). Они живут в одном файле стилей и делят часть
классов, поэтому у редизайна есть жёсткие границы.

**Эти классы запрещено стилизовать глобально** — они встречаются
одновременно в клиентской и тренерской части, и правка без префикса
мгновенно ломает вторую сторону:

| Класс | Где ещё встречается |
|---|---|
| `.section-title` | Прогресс, Замеры, История, Админ, карточка клиента |
| `.no-data` | Замеры, История, Админ, Тренеры, карточка клиента |
| `.stats-grid` | Прогресс и карточка клиента |
| `.exercise-select` | Прогресс, Замеры, карточка клиента |
| `.cc-history-container` | История и карточка клиента |
| `.ex-editor-input` | все модалки редактора у тренера |
| `.ex-editor-label` | там же |
| `.cc-back-btn` | карточка клиента, мастер, модалка статистики |

Правило: стилизовать их только с префиксом экрана —
`#progress-tab .section-title`, `#measurements-tab .no-data` и так далее.

**Клиентские экраны пишутся с привязкой к контейнеру.** Правила экрана живут
под `#home-tab`, `#workout-tab`, `#progress-tab` и так далее либо под
уникальным префиксом класса (`.home-*`). Новые стили клиента не должны
менять тренерскую часть — это проверяется замером: открыть тренерский экран
до и после правки и сравнить вычисленные стили.

**`.tabs` — общий элемент.** Панель навигации одна на всех: у клиента в ней
пять пунктов, у тренера добавляются «Админ» и «Тренеры». Любая правка
`.tabs`, `.tab-btn`, `.tab-ico` затрагивает оба интерфейса, и это нормально
— но делать её нужно осознанно, а не походя.

Осознанно общими сейчас являются ровно четыре вещи: шрифт `body`, шрифт
полей и кнопок, панель навигации и нижний отступ `body`.

### 2.11 Два цветовых токена: чей какой

Их легко перепутать, а последствия разные.

| Токен | Чей | Кто ставит |
|---|---|---|
| `--color-primary`, `--color-primary-dark` | **фирменный цвет тенанта** | `applyTenantTheme()` из `theme.primary` |
| `--accent-50…700`, `--accent-rgb` | **UI-акцент клиентского интерфейса** | `_applyAccentScale()` — выводится из того же `theme.primary` |

`--color-primary` — это механизм фирменного цвета тренера: им красятся
тренерские экраны и градиенты `--grad-red-*`. Вторым способом красить
клиентский интерфейс он быть не должен.

`--accent-*` — единственная система цвета клиентских экранов. Ни один
клиентский селектор не пишет акцентный хекс напрямую и не берёт
`--color-primary`.

Правила шкалы:

- строится из `theme.primary`, поэтому у каждого тренера свой цвет;
- если у тренера цвет не задан — inline-значения снимаются и в силу
  вступают значения по умолчанию из `:root` (петроль);
- при переключении тенанта шкала пересобирается целиком, цвет предыдущего
  тренера не остаётся;
- **тема влияет только на яркость, а не на сам цвет**: на светлом фоне
  слишком светлый цвет затемняется, на тёмном слишком тёмный подсвечивается,
  а мягкие ступени становятся полупрозрачными. Отдельного акцента для тёмной
  темы не существует и заводить его нельзя — объявление `--accent-*` внутри
  `body.theme-*` перебивает значение, которое JS ставит на `html`, и
  фирменный цвет тренера перестаёт работать;
- `--accent-rgb` — тот же цвет покомпонентно, для свечений и подложек, где
  нужна прозрачность;
- `--text-on-accent` подбирается автоматически: белый, а для очень светлого
  фирменного цвета — тёмный.

### 2.12 Переходный префикс `--ds-*`

Пока система внедряется, целевые цвета поверхностей и текста живут в CSS под
префиксом `--ds-` (`--ds-page-bg`, `--ds-surface`, `--ds-text` и т.д.) рядом
со старыми. Это сделано, чтобы добавление токенов не перекрасило приложение
разом: старые имена продолжают работать, новые ждут своего экрана.

Порядок: переносим экран → его правила переключаются на `--ds-*` → когда
старых имён не осталось нигде, значения `--ds-*` переезжают в основные имена,
а префикс убирается. Токены акцента, отступов, радиусов, теней и шрифта
префикса не имеют — они новые и ни с чем не конфликтуют.

---

## 3. Шрифт

**Гарнитура:** Inter (вариант — Manrope). Один шрифт на всё приложение.
Засечных заголовков больше нет.

**Подключение:** файл `.woff2`, только кириллица + латиница, кладём в
`fitness-app/vendor/`. **Никаких внешних CDN и Google Fonts** — по той же
причине, по которой все скрипты уже self-host: недоступный внешний хост
ломает приложение целиком.

```
font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
```

Начертания: 400 (regular), 500 (medium), 600 (semibold), 700 (bold). Больше
не грузим — каждое начертание это лишний вес.

### 3.1 Шкала размеров

| Роль | Размер / межстрочный | Вес | Где |
|---|---|---|---|
| `display` | 26 / 32 | 700 | «Привет, Анастасия!», заголовок героя |
| `h1` | 20 / 26 | 700 | заголовок экрана в шапке |
| `h2` | 18 / 24 | 700 | заголовок секции: «Твои результаты», «Техника выполнения» |
| `h3` | 16 / 22 | 600 | заголовок карточки, название упражнения |
| `body` | 15 / 21 | 500 | основной текст |
| `body-sm` | 14 / 20 | 500 | вторичный текст, строки списков |
| `caption` | 13 / 18 | 500 | подписи над цифрами, описания |
| `micro` | 11 / 14 | 600 | подписи навигации, мелкие чипы, оси графика |
| `num-xl` | 26 / 30 | 700 | главная цифра стат-карточки |
| `num-l` | 20 / 24 | 700 | цифра в компактной карточке |
| `num-m` | 17 / 22 | 600 | цифра в поле ввода |

**Правила:**
- Размеров вне этой таблицы не существует. Сейчас в `style.css` встречаются
  10, 11, 12, 13, 14, 15, 16, 17, 18, 20, 22, 24, 28, 32 px — при переносе
  экрана каждый прижимается к ближайшей ступени.
- Межбуквенный интервал: у `display` и `num-xl` — `-0.02em` (крупный текст
  плотнее); у `micro`-подписей в стат-карточках — `0` (в отличие от прошлого
  бежевого макета, вразрядку заглавными **больше не пишем**).
- Заглавными не пишем ничего, кроме коротких статус-чипов.
- Цифра и её подпись — разного цвета: цифра `--text`, подпись `--text-muted`.

---

## 4. Отступы

Шкала кратна 4. Других значений нет.

| Токен | Значение |
|---|---|
| `--gap-1` | 4px |
| `--gap-2` | 8px |
| `--gap-3` | 12px |
| `--gap-4` | 16px |
| `--gap-5` | 20px |
| `--gap-6` | 24px |
| `--gap-8` | 32px |

**Правила применения:**

| Ситуация | Отступ |
|---|---|
| Поля экрана слева/справа | 16 |
| Между секциями (заголовок секции и то, что выше) | 24 |
| От заголовка секции до её содержимого | 12 |
| Между карточками одного списка | 12 |
| Внутренние поля крупной карточки | 16 |
| Внутренние поля компактной карточки (стат-плитка, чип-строка) | 14 |
| Между иконкой и текстом | 8 |
| Между цифрой и подписью внутри плитки | 4 |
| Отступ снизу контента до нижней навигации | 16 + высота навигации |

Сетка плиток: 2 колонки, промежуток 12, `grid-template-columns: 1fr 1fr`.
Три плитки в ряд (экран «Прогресс») — промежуток 10, чтобы цифры не жались.

---

## 5. Скругления

| Токен | Значение | Где |
|---|---|---|
| `--radius-xs` | 8px | мелкие иконочные подложки, бейджи |
| `--radius-sm` | 10px | чипы метрик, мелкие кнопки |
| `--radius-md` | 12px | поля ввода, миниатюра упражнения, вложенные блоки |
| `--radius-lg` | 16px | **основной радиус карточек** |
| `--radius-xl` | 20px | герой-карточка с фото, крупные кнопки, видео-превью |
| `--radius-pill` | 999px | сегмент-контрол, круглые кнопки-иконки, прогресс-бары |

Правило вложенности: у вложенного блока радиус **на ступень меньше**, чем у
родителя. Карточка 16 → блок внутри 12 → чип внутри него 10.

---

## 6. Тени

| Токен | Значение | Где |
|---|---|---|
| `--shadow-card` | `0 1px 2px rgba(16,18,22,.04), 0 4px 12px rgba(16,18,22,.05)` | обычная карточка |
| `--shadow-raised` | `0 2px 4px rgba(16,18,22,.05), 0 8px 24px rgba(16,18,22,.07)` | активный сегмент, всплывающие элементы, модалки |
| `--shadow-accent` | `0 4px 14px rgba(14,90,86,.26)` | акцентная кнопка действия |
| `--shadow-nav` | `0 -6px 20px rgba(16,18,22,.05)` | нижняя навигация |

**Правила:**
- Тень всегда из двух слоёв: короткий плотный + длинный размытый. Одиночная
  тень выглядит дёшево.
- Тень никогда не чёрная в лоб — база `rgba(16,18,22,…)`, прозрачность не
  выше 0.08.
- Тень есть либо тень, либо граница — **не одновременно**, кроме карточек
  на белом фоне поверх белого, где нужна еле заметная `--border-soft`.
- Никаких `inset`-теней.

---

## 7. Иконки

**Набор:** Lucide (лицензия MIT, бесплатно). Один набор на всё приложение.

| Параметр | Значение |
|---|---|
| Толщина линии | 1.75px |
| Размер в навигации | 24px |
| Размер в кнопках и заголовках | 20px |
| Размер в чипах и строках | 16px |
| Цвет | `currentColor` — иконка всегда наследует цвет текста рядом |

**Правила:**
- Иконки вставляются как инлайн-SVG, не картинками и не иконочным шрифтом.
  Тогда они автоматически перекрашиваются под тему.
- **Эмодзи запрещены в навигации, кнопках, чипах, таблицах.** Сейчас вкладки
  — 🏠 💪 📊 📜 📏; это заменяется на линейные иконки. Эмодзи выглядят
  по-разному на iPhone и Android и мгновенно читаются как «сделано наспех».
- Единственное исключение — **декоративный акцент**: 🔥 у сегодняшней
  тренировки, 🏆 у рекорда, 🎯 у цели, 👋 в приветствии. Не больше одного
  эмодзи на карточку и никогда в интерактивных элементах.
- Иконка в стат-карточке сидит на подложке 28×28 с радиусом 8 и фоном
  `--surface-2` (или `--accent-50`, если карточка акцентная).
- Стрелка «дальше» в кликабельных строках — `chevron-right` 18px цветом
  `--text-faint`, прижата к правому краю.

---

## 8. Кнопки

| Тип | Высота | Радиус | Заливка | Текст |
|---|---|---|---|---|
| Основная, крупная | 52 | 20 | градиент `accent-300 → accent-600`, 135° | белый, 16/700 |
| Основная, обычная | 44 | 16 | тот же градиент | белый, 15/600 |
| Поверх фото | 44 | `pill` | сплошной `--accent-500` | белый, 15/700 |
| Вторичная | 44 | 14 | `--surface` + `1px --border` | `--text`, 15/600 |
| Тихая (ghost) | 40 | 12 | прозрачная | `--text-muted`, 14/600 |
| Чип / переключатель | 36 | 12 | `--surface` + `1px --border` | `--text-muted`, 13/500 |
| Чип выбранный | 36 | 12 | `--accent-50` + `1px --accent-200` | `--text`, 13/600 |
| Круглая иконочная | 40×40 | `pill` | `--surface-2` | иконка `--text` 20px |

**Правила:**
- Основная кнопка на экране одна. Две акцентных кнопки рядом — ошибка.
- У основной кнопки — `--shadow-accent`. У остальных теней нет.
- Ширина основной кнопки — во всю доступную ширину, минус поля экрана.
- Иконка внутри кнопки слева от текста, отступ 8.
- Мин. область нажатия любого интерактивного элемента — 44×44, даже если
  визуально он меньше.

### 8.1 Состояния

| Состояние | Оформление |
|---|---|
| Нажатие | `transform: scale(.985)`, прозрачность .92, переход 120ms |
| Заблокировано | прозрачность .45, тень убрана, курсор не меняем |
| Загрузка | текст заменяется на спиннер, ширина кнопки фиксируется |
| Фокус с клавиатуры | контур 2px `--accent-500`, отступ 2px |

Переходы: 150ms `ease-out` для цвета и прозрачности, 120ms для нажатия.
Длиннее 200ms в интерфейсе не делаем — начинает казаться тормозящим.

---

## 9. Нижняя навигация

Заменяет нынешние верхние прилипшие вкладки.

| Параметр | Значение |
|---|---|
| Позиция | `fixed`, низ экрана |
| Высота | 56 + `env(safe-area-inset-bottom)` |
| Фон | `--surface` |
| Верхняя граница | 1px `--border-soft` |
| Тень | `--shadow-nav` |
| Пунктов | 5 максимум, равными долями |
| Иконка | 24px, линейная |
| Подпись | `micro` 11/600, отступ от иконки 4 |
| Неактивный | иконка и текст `--text-muted` |
| Активный | иконка и текст `--accent-500` |

**Правила:**
- Пять пунктов и ни одним больше. Всё остальное уходит под «Ещё».
- У тренера состав пунктов другой, но их тоже пять — админка занимает место
  одного из клиентских разделов, а не добавляется шестой.
- Подложек, капсул и заливок под активным пунктом нет — только цвет.
- Учёт `env(safe-area-inset-bottom)` обязателен, иначе на iPhone панель
  налезает на системную полосу.
- Контент страницы получает нижний отступ, равный высоте панели + 16.

---

## 10. Стат-карточки

Плитка с одним числом. Основной строительный блок «Главной» и «Прогресса».

| Параметр | Значение |
|---|---|
| Фон | `--surface` |
| Радиус | `--radius-lg` (16) |
| Поля | 14 |
| Граница | 1px `--border-soft` |
| Тень | `--shadow-card` |
| Мин. высота | 92 |

Порядок содержимого сверху вниз:
1. Строка «иконка + подпись»: иконка 16px на подложке 28×28 (радиус 8), рядом
   подпись `caption` цветом `--text-muted`. Отступ снизу 8.
2. Число: `num-xl` (26/700) цветом `--text`. Единица измерения — тем же
   размером, но весом 600 и на 2px меньше.
3. Динамика: `micro` со стрелкой, цвет `--fg-success` / `--fg-danger`.
   Отступ сверху 4. Если динамики нет — строка не рисуется, высота карточки
   не скачет (держим `min-height`).

Акцентный вариант (выбранный рекорд, текущая цель): фон
`linear-gradient(135°, --accent-50, --accent-100)`, граница 1px
`--accent-200`, тень убирается.

---

## 11. Карточка упражнения

| Параметр | Значение |
|---|---|
| Фон | `--surface`, радиус 16, поля 14, тень `--shadow-card` |
| Миниатюра | 56×56, радиус 12, `object-fit: cover` |
| Название | `h3` (16/600), в одну строку с обрезкой многоточием |
| Статус-чип справа | высота 24, радиус `pill`, фон `--surface-2`, текст `micro` `--text-muted` |
| Строка метрик | отступ сверху 12, чипы в ряд с промежутком 8, перенос разрешён |
| Чип метрики | высота 32, радиус 10, `--surface` + 1px `--border`, иконка 14 + текст 13/500 |

**Правила:**
- Метрики всегда в одном порядке: подходы → повторения → вес → RPE.
- Пустая метрика не рисуется вовсе (не «— кг»).
- Вся карточка кликабельна целиком, а не отдельная стрелка.
- Активное (текущее) упражнение получает границу 1px `--accent-200` и фон
  `--accent-50`; тень при этом убирается.

### 11.1 Ввод результата

Поля «Подходов / Повторений / Вес»: высота 56, радиус 12, фон `--surface`,
граница 1px `--border`. Внутри сверху подпись `micro` `--text-faint`, снизу
значение `num-m`. Кнопки «−» и «+» — 28×28, радиус 8, фон `--surface-2`,
прижаты к краям поля.

Оценка «Как было?»: 4 равные кнопки-чипа с иконкой-смайлом 20px сверху и
подписью `micro` снизу; высота 64, радиус 12. Выбранная — `--accent-50` +
граница `--accent-200` + галочка вместо смайла.

---

## 12. Графики (Chart.js)

Библиотека уже подключена (`vendor/chart.umd.min.js`), нужен только стиль.

| Параметр | Значение |
|---|---|
| Тип | линия со сглаживанием, `tension: 0.4` |
| Цвет линии | `--accent-500`, толщина 2.5 |
| Заливка под линией | вертикальный градиент `rgba(22,112,107,.16)` → `rgba(22,112,107,0)` |
| Точки | радиус 3.5, заливка `--accent-500`, обводка белая 2px |
| Последняя точка | радиус 5, заливка `--ink` — она подписана тултипом |
| Горизонтальная сетка | выключена |
| Вертикальная сетка | 1px `#F0F0F3`, сплошная |
| Оси | линии осей выключены, засечки выключены |
| Подписи осей | `micro` (11/600) цветом `--text-faint` |
| Тултип | фон `--ink`, радиус 8, поля 6×10, текст белый 12/600, без стрелки |
| Легенда | выключена — вместо неё заголовок и выпадающий список над графиком |
| Анимация | 400ms `easeOutQuart`, только при первой отрисовке |

**Правила:**
- Максимум две линии на графике. Нужна третья — это уже другой график.
- Второй ряд, если он есть, — серый `--text-faint`, без заливки.
- Столбчатый график: радиус верхних углов 6, ширина столбца не больше 60%
  шага, цвет `--accent-300`, активный столбец `--accent-500`.
- Кольцевая диаграмма (КБЖУ): толщина кольца 10, скругления концов, дорожка
  `--surface-2`.
- Прогресс-бар: высота 8, радиус `pill`, дорожка `--surface-2`, заливка
  `linear-gradient(90°, --accent-300, --accent-500)`.

---

## 13. Градиенты — правила

Градиент разрешён ровно в четырёх местах:

1. **Основная кнопка** — 135°, `accent-300 → accent-600`.
2. **Акцентная плашка** (цель, достижение, выбранный элемент) — 135°,
   `accent-50 → accent-100`. Разница между стопами еле заметна.
3. **Заливка под линией графика** — вертикально, из цвета линии в прозрачность.
4. **Затемнение поверх фотографии** — см. раздел 14.

**Запрещено:**
- градиент на обычной карточке, на фоне экрана, на нижней навигации;
- градиент в тексте;
- больше двух стоп-точек;
- разница между стопами больше 12% по светлоте (кроме затемнения фото);
- два разных градиента в одном экране рядом.

Направление всегда 135° (кнопки, плашки) или 180°/90° (заливки, прогресс).
Произвольных углов нет.

---

## 14. Фотографии — правила

Фото допускаются ровно в трёх местах:

| Место | Пропорции | Радиус |
|---|---|---|
| Герой на «Главной» | 16:9 | `--radius-xl` (20) |
| Миниатюра упражнения | 1:1 | `--radius-md` (12) |
| Превью видео техники | 16:9 | `--radius-lg` (16) |

**Правила:**
- Поверх фото пишем текст только через затемнение:
  `linear-gradient(100°, rgba(18,16,13,.82) 0%, rgba(18,16,13,.45) 55%, rgba(18,16,13,.1) 100%)`
  для текста слева. Без затемнения текст на фото не размещаем никогда —
  фото меняются, а текст обязан остаться читаемым.
- Кадрирование `object-fit: cover`, фокус по центру. Растянутых и сплюснутых
  изображений быть не может.
- Формат `webp`, ширина в 2 раза больше отрисовки (герой — 1500px).
  Обязательны `width`/`height` в разметке, иначе экран прыгает при загрузке.
- Если фото нет — рисуем заглушку: фон `--surface-2` + иконка 24px цветом
  `--text-faint` по центру. Логотип тренера в качестве заглушки не тянем.
- Кнопка «play» на видео-превью: круг 56, фон `rgba(255,255,255,.92)`,
  треугольник `--text`, тень `--shadow-raised`.
- Фото упражнений хранятся в базе, не на диске приложения (диск на Timeweb
  временный и очищается при выкатке).
- Аватар: круг, 44 в шапке, 36 в списках, граница 2px `--surface`.

---

## 15. Экраны, которых нет в макете

Тренерская, суперадминка, таблицы, модалки — оформляются теми же токенами.
Отличия только в плотности:

- Строка таблицы: высота 44, разделитель 1px `--border-soft`, текст `body-sm`.
- Заголовок столбца: `micro` `--text-muted`.
- Модальное окно: радиус 20 сверху (шторка снизу), фон `--surface`,
  тень `--shadow-raised`, поля 16, «ручка» 36×4 радиус `pill` цветом
  `--surface-4` по центру сверху.
- Пустое состояние: иконка 32 `--text-faint`, заголовок `h3`, пояснение
  `body-sm` `--text-muted`, отступ сверху 32, по центру.
- Скелетон загрузки: блок `--surface-2` с радиусом как у настоящего элемента,
  переливание 1.2s. Спиннеры — только внутри кнопок.

---

## 16. Порядок внедрения

Дизайн-система вводится по частям, каждый шаг выкатывается отдельно через
`bump-version.sh` и проверяется на телефоне.

1. Токены (разделы 2–6) — добавить переменные, ничего визуально не меняя.
2. Шрифт (раздел 3) + иконки (раздел 7).
3. Нижняя навигация (раздел 9).
4. Главная: герой, неделя, стат-карточки, цель.
5. Тренировка: карточка упражнения, ввод результата.
6. Прогресс: сегмент-контрол, график, рекорды, достижения.
7. Тренерские экраны и модалки (раздел 15).
