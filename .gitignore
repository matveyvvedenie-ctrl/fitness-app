node_modules/
# build/ — генерируется `npm run build` перед выкладкой на VK-хостинг.
# В git не держим: закоммиченная сборка протухает молча (лежала версия от
# 18.08 с <script src="app.js"> без ?v=, то есть с вечным кэшем у людей).
build/
